[tool.poetry]
name = "scoring-credit"
version = "0.1.0"
description = "TP1 MLOps - Du prototype au modele servi"
authors = ["Libas"]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10,<3.13"
pandas = "^2.2"
scikit-learn = "^1.5"
mlflow = "^2.16"
dvc = "^3.55"
fastapi = "^0.115"
uvicorn = "^0.30"
pydantic = "^2.9"
joblib = "^1.4"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"